/* ===============================
   前期设定 - 生成脑洞
   =============================== */
const PROMPT_IDEA = (world) => `
根据以下创意概述，扩展成完整的小说设定与核心脑洞：

${world}
`;


/* ===============================
   全书大纲生成
   =============================== */
const PROMPT_OUTLINE = (preset) => `
根据以下小说设定与脑洞，生成完整的全书结构大纲。

【小说设定】
${preset}
`;


/* ===============================
   卷纲 - 批量生成卷简介
   =============================== */
const PROMPT_VOLUME_DESC = (outline, totalVolumes) => `
根据以下全书大纲，将小说划分为 ${totalVolumes} 卷。

【全书大纲】
${outline}

请按以下格式依次输出每一卷简介：

第1卷：
（简介内容）

第2卷：
（简介内容）

依次类推，不要省略。
`;


/* ===============================
   卷纲 - 生成卷大事件
   =============================== */
const PROMPT_VOLUME_EVENTS = (desc) => `
根据以下信息，设计本卷的关键事件节点。

【卷简介】
${desc}

请按时间顺序列出本卷的主要事件节点。
`;


/* ===============================
   章节细纲 - 生成章节概述
   =============================== */
const PROMPT_CHAPTER_OUTLINE = (events, chapterCount) => `
根据以下本卷事件节点，将内容拆分为 ${chapterCount} 章。

【本卷事件节点】
${events}

要求：
1. 每一章写一个章节概述
2. 每章概述控制在400字以内
3. 按时间顺序拆分
4. 不要写正文

输出格式：
第1章：
（概述）

第2章：
（概述）

依次类推
`;


/* ===============================
   章节细纲 - 场景拆分
   =============================== */
const PROMPT_SCENE = (summary) => `
根据以下章节概述，将内容拆分为3-5个场景。

【章节概述】
${summary}

每个场景请包含：
1. 时间/地点
2. 出场人物
3. 场景目标
4. 冲突核心
5. 场景结果变化

输出格式：
场景1：
（内容）

场景2：
（内容）
`;


/* ===============================
   章节细纲 - 剧情节拍
   =============================== */
const PROMPT_BEATS = (summary, scene) => `
根据以下内容，为本章生成固定6个剧情节拍。

【章节概述】
${summary}

【场景拆分】
${scene}

要求：
1. 必须严格输出6个节拍
2. 每个节拍包含行动或冲突
3. 必须推动剧情
`;


/* ===============================
   正文生成
   =============================== */
const PROMPT_TEXT = (summary, scene, beats) => `
请根据以下信息创作本章小说正文，字数2200左右。

【章节概述】
${summary}

【场景拆分】
${scene}

【剧情节拍】
${beats}

要求：
1 严格按节拍顺序
2 不要写说明
3 使用小说叙事
`;
