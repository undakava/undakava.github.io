/* ===============================
   前期设定 - 生成脑洞
   =============================== */
window.PROMPT_IDEA = (world) => `
根据以下创意概述，扩展成完整的小说设定与核心脑洞：

${world}
`;


/* ===============================
   全书大纲生成
   =============================== */
window.PROMPT_OUTLINE = (preset) => `
根据以下小说设定与脑洞，生成完整的全书结构大纲。

【小说设定】
${preset}
`;


/* ===============================
   卷纲 - 批量生成卷简介
   =============================== */
window.PROMPT_VOLUME_DESC = (outline, totalVolumes) => `
根据以下全书大纲，将小说划分为 ${totalVolumes} 卷。

【全书大纲】
${outline}

请按以下格式依次输出每一卷简介：

第1卷：
（简介内容）

第2卷：
（简介内容）

依次类推，不要省略。
`;


/* ===============================
   卷纲 - 生成卷大事件
   =============================== */
window.PROMPT_VOLUME_EVENTS = (desc) => `
根据以下信息，设计本卷的关键事件节点。

【卷简介】
${desc}

请按时间顺序列出本卷的主要事件节点。
`;


/* ===============================
   章节细纲 - 生成章节概述
   =============================== */
window.PROMPT_CHAPTER_OUTLINE = (events, chapterCount) => `
根据以下本卷事件节点，将内容拆分为 ${chapterCount} 章。

【本卷事件节点】
${events}

要求：
1. 每一章写一个章节概述
2. 每章概述控制在800字左右
3. 按事件发展拆分
4. 不要写正文
5. 每两章发生一个小的核心冲突
6. 每章结尾留下钩子

输出格式：
第1章：
（概述）
（核心冲突）
（钩子）

第2章：
（概述）
（核心冲突）
（钩子）

依次类推
`;


/* ===============================
   章节细纲 - 场景拆分
   =============================== */
window.PROMPT_SCENE = (summary) => `
根据以下章节概述，将内容拆分为3-5个场景。

【章节概述】
${summary}

每个场景请包含：
1. 时间/地点
2. 出场人物
3. 场景目标
4. 冲突核心
5. 场景结果变化

输出格式：
场景1：
（内容）

场景2：
（内容）
`;


/* ===============================
   章节细纲 - 剧情节拍
   =============================== */
window.PROMPT_BEATS = (summary, scene) => `
根据以下内容，为本章生成固定12个剧情节拍。

【章节概述】
${summary}

【场景拆分】
${scene}

要求：
1. 必须严格输出12个节拍
2. 每个节拍包含行动或冲突
3. 必须推动剧情
`;


/* ===============================
   正文生成
   =============================== */
window.PROMPT_TEXT = (summary, scene, beats) => `
请根据以下信息创作本章小说正文，字数4400左右。

【章节概述】
${summary}

【场景拆分】
${scene}

【剧情节拍】
${beats}

 ## 角色设定
 你是一位专业的番茄小说写手，擅长创作符合番茄小说平台读者喜好的爽文正文。你深谙番茄小说的核心特点：快节奏、高爽感、直白易懂、代入感强、爽点密集。你能够精准把握读者期待，创作出让读者欲罢不能的正文。

 ### 番茄小说读者期待（核心要点）
 快节奏：不拖沓，情节推进快，让读者一直想往下看
 高爽感：打脸、装逼、收美女、发财、变强，爽点密集
 直白易懂：语言简洁直白，不要文艺腔，接地气
 代入感强：让读者能快速代入主角，感同身受
 冲突明确：矛盾冲突要明确，敌我分明
 情绪饱满：要有情绪起伏，让读者跟着主角一起爽

 ## 任务目标
 根据用户提供的对标小说正文、前面已写的章节正文、本次写作剧情点和题材类型选择，生成符合对标小说风格、逻辑严密、人设不崩塌的正文。生成的正文必须严格按照用户提供的本次写作剧情点进行，结尾必须在用户提供的最后一个剧情点戛然而止，严禁在最后一个剧情点之后添加任何新剧情、新对话、新场景或任何延伸内容。

 【章节数量强制要求】：用户可能提供1章、2章或3章的剧情点。必须先统计用户输入中"第X章"标题的数量，有几个标题就必须生成几章正文。严禁只生成一章就结束。

 ### 冲突处理优先级（当出现冲突时按此顺序处理）

 优先级从高到低：
 1. 剧情点（最高优先级）
 2. 题材类型的世界观规则
 3. 对标小说风格
 4. 标签要求

 （以下内容保持原样，不做任何改动）
 ...
`;